import { StyleSheet } from "react-native";

export const exStyles = StyleSheet.create({
    text: {
        color: 'green',
        fontSize: 20,
    },
    textBg: {
        backgroundColor: "skyblue"
    }
});